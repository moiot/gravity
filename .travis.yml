language: go

go:
  - "1.11"

install: true

services:
  - docker

jobs:
  include:
    - name: "build"
      script: make build-linux
    - name: "test"
      script: make test
      after_script: make test-down